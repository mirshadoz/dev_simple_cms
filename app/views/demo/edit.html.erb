<%= form_tag(action: 'update') do %>
	<%= label_tag('email','Email') %><br/>
	<%= text_field_tag('email', params[:email]) %><br/>

	<%= label_tag('password','Password') %><br/>
	<%= password_field_tag('password', params[:password]) %><br/></br>

	<%= submit_tag('Submit') %>
<% end %>